on: [push]

jobs:
  check_spell_job:
    runs-on: ubuntu-latest
    name: Check English prose
    steps:
      - name: Check spelling
        id: spell_check
        uses: mnahinkhan/writegood-action@37f3a35
